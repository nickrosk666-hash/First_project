Refactor the current codebase for cleaner code:

1. Read all source files in `app/` and any other project directories (excluding `node_modules`)
2. Identify issues: duplicated code, poor naming, unnecessary complexity, unused imports, inconsistent patterns
3. Rewrite the code to be cleaner while preserving all existing functionality
4. Ensure TypeScript types are correct and Tailwind classes are consistent
5. Show a summary of what was changed and why
